#ifndef CUSTOM_LIB
#define CUSTOM_LIB

unsigned int strContains(const char *, const char);

const char* substring(const char *, unsigned int, unsigned int);

unsigned int _isAlpha(const char);

unsigned int _isDigit(const char);

#endif